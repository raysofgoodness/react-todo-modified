import './search.css';

const Search = () => {
    return (
        <input type="text"
            className="form-control search-input"
            placeholder="Search employee"/>
    );
}

export default Search;
